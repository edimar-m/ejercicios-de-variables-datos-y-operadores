console.log("Área del trángulo");
//A=b*h/2

const base = prompt("Ingrese la base del triángulo");

const altura = prompt("Ingrese la altura del triangulo");

const area = (base*altura)/2;

alert(`El área del triángulo es de ${area}`);