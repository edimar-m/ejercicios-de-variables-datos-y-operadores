console.log("Perímetro del rectangulo");

//Perímetro del rectangulo 2*(ancho+alto)

const altura = Number(prompt("Ingrese la altura del rectangulo"));

const ancho =  Number(prompt("Ingrese el ancho del rectangulo"));

const perimetro = 2*(ancho+altura);

alert(`El perimetro del rectangulo es de ${perimetro}`);